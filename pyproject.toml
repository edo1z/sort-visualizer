[project]
name = "sort-visualizer"
version = "0.1.0"
description = "ソートアルゴリズムを GIF で可視化する図鑑"
requires-python = ">=3.10"
dependencies = [
    "pillow>=10.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
addopts = "-ra"
pythonpath = ["."]
